on:
  push:
    tags:
      - 'en-gui-*'

jobs:
  build_job:
    runs-on: ubuntu-latest
    name: Builds OVOS
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          submodules: 'recursive'
      - name: Build
        id: build
        uses: ccoreilly/OpenVoiceOS@catala
        with:
          make-targets: 'clean rpi4_64-gui-config rpi4_64-gui'
      - name: Release
        uses: softprops/action-gh-release@v1
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: release/OpenVoiceOS_rpi4_64-gui.image.xz
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}